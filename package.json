{
  "name": "website-starter-with-parcel",
  "version": "0.0.1",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "source": "src/index.html",
  "scripts": {
    "start": "parcel serve",
    "build": "parcel build --no-source-maps",
    "build:dev": "parcel build",
    "cleanup": "rimraf dist"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "2.8.3",
    "parcel": "^2.8.3",
    "rimraf": "^4.1.2",
    "sharp": "^0.31.1"
  },
  "dependencies": {}
}
